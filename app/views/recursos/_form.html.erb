<%= form_for(@recurso) do |f| %>
  <% if @recurso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recurso.errors.count, "error") %> prohibited this recurso from being saved:</h2>

      <ul>
      <% @recurso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :serial %><br />
    <%= f.number_field :serial %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
